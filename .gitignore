# Node
node_modules/
next-env.d.ts
.next/
out/
tsconfig.tsbuildinfo

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.mypy_cache/
.ruff_cache/
.venv/
venv/
venv_tmp/

# Poetry
poetry.lock

# Docker
Dockerfile.cache

# Env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log
logs/

# OS
Thumbs.db

# Testing
coverage/
.nyc_output/

# Drizzle
drizzle/
migrations/

# Alembic
alembic/versions/.gitkeep

# misc
*.pem
*.key
*.crt

# Local storage (certificates, generated PDFs, etc.)
# Only the apps/api/storage/ runtime dir is excluded; the app/core/storage
# package is source code and must be tracked.
apps/api/storage/

.vercel
check_*.sql
check_*.sql
fix_*.sql
add_*.sql
test_demo.json

# Generated artefacts (don't commit)
repomix-output*.md
.claude/
.aider*

# Playwright session storage and report outputs (per-machine, never commit)
.playwright/
.playwright-sidebar.png
.reports/

# Per-developer AI session notes (rendered guides, scratch analysis, etc.)
docs/agents/
docs/analysis/
docs/prompts/

# Render CLI debug captures (render logs ... > .render-logs.json).
# Regenerated every time we run `render logs --output json` for diagnostics;
# not part of the project source.
.render-logs*.json
.render-logs*.txt

# Runtime caches — never commit
apps/api/summaries/

.env*.local
# one-off deploy scripts with embedded secrets — must NEVER commit
*.deploy.sh

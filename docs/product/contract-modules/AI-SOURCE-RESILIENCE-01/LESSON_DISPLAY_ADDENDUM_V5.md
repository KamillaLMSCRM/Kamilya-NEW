# Generated lesson display V5

Status: Accepted by Astra root, 2026-09-10, within full-course live acceptance.
Supersedes: EPIC_V1 no-UI exclusion only for safe lesson text rendering.

Browser acceptance of the generated Excel course revealed visible Markdown
heading/list/table syntax. The existing SafeLessonContent parses only inline bold
and emphasis. The source itself was saved successfully (3 modules/6 lessons/26 MCQ).

Interface: SafeLessonContent({text:string}) -> React nodes. Extract a dedicated
safe renderer used by the existing course player. Support headings, paragraphs,
ordered/unordered lists, fenced code and simple pipe tables generated by the writer;
retain inline bold/emphasis and plain-text fallback. Render user text as React text
only, never raw HTML/dangerouslySetInnerHTML; no remote resources or new dependencies.
Malformed or unsupported input remains readable text, not dropped content.
Headings within the lesson must not replace the page-level title. Tables may scroll
horizontally on narrow screens. Preserve all content, not just a prefix.

Ownership: Terra writes only the new component and its focused tests. Root owns
the one course-page import/replacement, integration and browser acceptance.
Do not alter API calls, quiz interaction, completion, assistant, learner roles,
SCORM, routing, provider keys, settings or dependencies.

Checks: headings/lists/table/inline/plaintext, fenced HTML displayed as text,
injection strings never create script/img/iframe/link attributes, malformed input
retained, long text retained, existing player role and assignment tests unchanged.
This rendering correction requires frontend release/readback separately from API.

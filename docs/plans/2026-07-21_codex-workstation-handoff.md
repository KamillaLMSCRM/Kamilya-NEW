# Перенос разработки Kamilya LMS на другой компьютер

Дата: 2026-07-21

Статус: в работе

## Цель

Передать новому Codex не только Git-репозиторий, но и актуальную модель продукта, production-состояние, правила разработки, известные ограничения и безопасный порядок продолжения работы.

## План

1. [x] Проверить рабочее дерево, канонические документы и устаревшие точки входа.
2. [x] Создать самодостаточный `docs/CODEX_HANDOFF.md`.
3. [x] Обновить `AGENTS.md`, `README.md` и индекс документации.
4. [x] Проверить ссылки, отсутствие секретов и согласованность handoff с текущими документами.
5. [x] Закоммитить документацию автором проекта и отправить `HEAD` в `origin/master` без Git Credential Manager.
6. [ ] Проверить удалённый commit и завершить план.

## Пункт 2 — handoff

**Что сделано:** создан `docs/CODEX_HANDOFF.md` с моделью продукта, ролями, флоу, архитектурой, production topology, проверками, открытыми вопросами и инструкцией переноса.

**Статус:** ✅ done

## Пункт 5 — подготовка Git

**Что сделал:** подтвердил, что `HEAD` и `origin/master` до коммита совпадают на `2545eb3`, `.env` игнорируется, Git author установлен как `Kamilla LMS CRM <kamilla_lms_crm@proton.me>`. Для fetch/push используется `github_token` из `.env` через временный HTTP authorization header с отключёнными `credential.helper` и `core.askPass`.

**Проверки:** authenticated fetch `origin/master`, ancestor check, `git check-ignore .env`.

**Статус:** ✅ done; commit/push выполняются следующим действием.

## Пункт 3 — точки входа

**Что сделано:** `AGENTS.md` теперь отправляет нового агента в handoff и актуальные документы вместо исторического `TZ.md`; `README.md` приведён к `methodologist`, Valkey и Alembic `0068`; ссылка добавлена в индекс.

**Статус:** ✅ done

## Пункт 4 — проверка документации

**Что сделал:** проверил локальные Markdown-ссылки, stale role/infra/schema terms, отсутствие присваиваний секретов и `git diff --check`. Удалил последнее действующее упоминание `teacher` из RBAC-инструкции `AGENTS.md`; упоминания об удалённой роли в handoff сохранены как явный запрет на её восстановление.

**Проверки:** link scan, `rg`, secret-assignment scan, `git diff --check`.

**Статус:** ✅ done

## Ограничения

- `.env` и любые значения секретов не входят в Git; пользователь переносит `.env` отдельно.
- История Codex не копируется в репозиторий.
- Handoff описывает только подтверждённое состояние; исторические планы не выдаются за актуальный backlog.

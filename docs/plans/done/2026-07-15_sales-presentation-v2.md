# Sales-презентация Kamilya LMS v2

## Цель

Переработать интерактивный HTML walkthrough в два режима: короткий продающий сценарий на 8 экранов и сохранённый полный продуктовый сценарий. Не добавлять неподтверждённые показатели, адаптивное обучение, юридическую силу сертификата или функции, отсутствующие в production.

## Пункт 1 — добавить короткий sales-story

- Режим `sales` сделать режимом по умолчанию.
- Последовательность: проблема → источник → AI-черновик → контроль методолога → публикация и назначение → обучение → результат → пилот.
- Использовать только проверенные продуктовые возможности.

**Статус:** completed — добавлен отдельный сценарий из 8 экранов, режим `sales` используется по умолчанию.

## Пункт 2 — снизить визуальную перегруженность

- В sales-режиме свернуть навигацию продукта.
- Убрать перекрывающие UI калл-ауты.
- Вынести объяснение в постоянную нижнюю панель `этап / действие / результат`.
- Оставить один главный визуальный акцент на экране.

**Статус:** completed — продуктовый UI свёрнут до рабочей области, пояснения перенесены в постоянную нижнюю панель.

## Пункт 3 — сохранить полный walkthrough

- Текущие 15 экранов оставить в режиме `?mode=full`.
- Добавить переключатель режимов.
- Обновить overview и счетчик под выбранный режим.

**Статус:** completed — 15 исходных экранов доступны через `?mode=full`, режимы переключаются кнопкой и клавишей `M`.

## Пункт 4 — улучшить навигацию и запись

- Синхронизировать `mode` и `slide` с URL.
- Добавить focus management и `inert` для overview.
- В режиме `record=1` полностью скрывать controls и курсор.
- Добавить корректные touch/safe-area настройки.

**Статус:** completed — URL хранит `mode/slide`, overview использует dialog/focus/inert, `record=1` скрывает controls и курсор.

## Пункт 5 — проверить и зафиксировать

- Playwright: sales/full, переключение режима, URL, overview, autoplay, recording.
- Скриншоты: 1920×1080 и 390×844.
- Проверить все сцены на overflow и консольные ошибки.
- Обновить README, завершить план, commit/push только файлов задачи.

**Статус:** completed — Playwright проверил desktop/mobile, 8/15 кадров, URL, overview, autoplay, recording и отсутствие ошибок консоли. Скриншоты визуально проверены.

## Итог проверки

- Desktop: `1920×1080`, все 8 sales-экранов просмотрены; рабочий UI и нижняя панель не перекрываются.
- Mobile: `390×844`, горизонтального overflow нет, отдельная подпись текущего этапа читаема.
- Full: все 15 сцен сохранили формат `1280×720`; intro/outro имеют декоративный overflow внутри собственного `overflow:hidden`.
- Recording: после 7,8 секунды автопоказ перешёл на экран 2; controls имеют `opacity: 0`, `pointer-events: none`, курсор скрыт.
- Console: 0 ошибок, 0 предупреждений.

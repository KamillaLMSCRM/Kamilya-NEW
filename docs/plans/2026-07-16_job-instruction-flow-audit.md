# Аудит флоу должностных инструкций

## Цель

Проверить сквозной сценарий работы с должностными инструкциями: создание должности и загрузка ДИ, извлечение/аудит содержания, генерация курса типа `job_description`, публикация и назначение сотрудникам по должности, прохождение, журнал и trial-лимит.

## Пункт 1 — сопоставить продуктовый контракт и реализацию

- Сверить `docs/TZ_JOB_INSTRUCTION_ONBOARDING_v1.md`, пользовательскую документацию и актуальные продуктовые описания.
- Построить карту frontend → API → service/model → database для каждого шага.
- Отделить реализованные функции от старых или неподтверждённых требований.

**Что сделал:** сопоставил `TZ_JOB_INSTRUCTION_ONBOARDING_v1.md`, презентацию, пользовательскую документацию, `/positions`, три API router, модели `Position`/`Course` и trial usage. Подтверждено, что текущая реализация хранит только обязанности/требования и не реализует заявленные `instruction_document_id` и `source_instruction_id`.

**Проверки:** поиск моделей, migrations, API endpoints и frontend-вызовов; построен фактический путь от файла до пустого черновика курса.

**Статус:** ✅ done

## Пункт 2 — проверить backend и данные

- Проверить tenant/RBAC, привязку документа к должности, версии ДИ и обработку ошибок.
- Проверить генерацию курса `job_description`, trial enforcement и идемпотентность назначений.
- Запустить релевантные unit/integration тесты и доступные production smoke-проверки без изменения пользовательских данных.

**Что сделал:** устранил три лишних stacked decorators, из-за которых JD URLs регистрировались на неверных handlers. Добавил role guard `superadmin/methodologist/teacher` на все три positions routers, сохранив tenant guard. Добавил контрактные тесты уникальности маршрутов и role dependency.

**Проверки:** `pytest` по JD routes, position-course links, assignment service и trial/AI dispatch — 24 passed; `compileall` успешно; OpenAPI строится, 184 paths.

**Статус:** ✅ done

## Пункт 3 — проверить UX по ролям

- Проверить, что сценарий находится у методолога, а не у администратора тенанта.
- Пройти экраны должностей, загрузки/анализа ДИ, рекомендаций и генерации курса.
- Проверить loading/error/empty/success states, мобильный viewport и переходы между экранами.

**Что сделал:** проверил Sidebar, основной экран и все modal/state branches. Зафиксировал перегрузку общего списка, разрыв «темы → пустые курсы → другой раздел», ложное обещание learner onboarding quiz, скрытые API errors, несимметричный upload diff и mobile/accessibility риски. Production route без активной сессии остался на auth loading, поэтому интерактивный визуальный проход не использовался как доказательство.

**Проверки:** статический аудит `positions/page.tsx` и Sidebar по актуальным Web Interface Guidelines; поиск реального потребителя `PositionQuiz` по backend/frontend.

**Статус:** ✅ done

## Пункт 4 — оформить вывод и дальнейшие действия

- Зафиксировать найденные разрывы по приоритетам P0/P1/P2 с файлами и воспроизведением.
- При наличии узких однозначных дефектов исправить их и покрыть тестами; крупные продуктовые изменения вынести в отдельный план.
- Обновить внутреннюю документацию фактическим флоу.

**Что сделал:** создал `docs/analysis/2026-07-16_job-instruction-flow-ux-audit.md` с оценками применимости/удобства/понятности, P0/P1/P2 findings и целевым state-driven flow.

**Проверки:** выводы привязаны к актуальным файлам и строкам; неподтверждённые функции отделены от работающих.

**Статус:** ✅ done; commit/push и production deploy ещё не выполнялись, поэтому план остаётся в `docs/plans/`.

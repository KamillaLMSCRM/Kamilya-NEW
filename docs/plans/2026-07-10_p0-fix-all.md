# P0 Fix-All — статус и проверка (2026-07-10)

## Итог

Production P0-блоки были исправлены в нескольких коммитах удалённого master и
дополнены локальной проверкой:

- RLS-контекст перед созданием tenant-scoped users;
- RLS-контекст перед созданием demo-user;
- удаление устаревшего SQLAlchemy crossjoin;
- устранение duplicate OpenAPI operation ID;
- UTF-8 BOM и charset для CSV;
- совместимый response schema для UUID в audit logs;
- создание tenant settings при регистрации;
- migration 0054 для course delivery_type.

## Проверка локального checkout

- backend targeted suite: 15 passed;
- backend unit suite: 17 passed;
- compileall: passed;
- TypeScript frontend check: passed;
- JSON locale validation: passed;
- в коде нет Select.crossjoin().

## Production verification

Удалённый P0-отчёт заявляет успешный smoke по регистрации, demo-login,
dashboard, audit logs, CSV, OpenAPI и tenant settings. Эти проверки нужно считать
подтверждёнными только после сверки Render deploy commit с текущим master и
повторного запроса к production без публикации credentials.

## Обязательные действия безопасности

- Временные QA credentials не хранятся в Git, отчётах или логах.
- Временный superadmin password необходимо ротировать.
- Redis credential, ранее попавший в историю/документацию, необходимо считать
  скомпрометированным и заменить.
- Upstash quota нужно поднять или заменить Redis provider: fallback не является
  надёжным shared storage для нескольких API replicas.

## Следующий smoke

1. Сверить Render commit с origin/master.
2. Проверить /health.
3. Проверить регистрацию на отдельном QA email.
4. Проверить demo-login в разрешённом non-production сценарии.
5. Проверить /admin/dashboard, /audit/logs и CSV после tenant impersonation.
6. Проверить OpenAPI без duplicate operation IDs.
7. Проверить, что migration 0054 применена.


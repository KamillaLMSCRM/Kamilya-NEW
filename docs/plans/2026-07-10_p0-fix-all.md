# P0 Fix-All — все critical production bugs (2026-07-10)

## Контекст

P1 QA-сессия 2026-07-10 нашла 9 багов в production (5 CRITICAL, 4 medium).
Askar дал команду "сам сделай все" — фикшу без ожидания подтверждения по каждому пункту.

## Что исправлено

| # | Bug | Endpoint | Fix |
|---|---|---|---|
| 1 | RLS tenant_isolation блокирует INSERT | `/auth/register` | `set_config('app.tenant_id')` в `create_user_and_tokens` |
| 2 | То же для demo-login (inline User create) | `/auth/demo-login` | same pattern в `demo_login` функции |
| 3 | SQLAlchemy 2.0 не имеет `Select.crossjoin()` | `/admin/dashboard` | 4 отдельных COUNT вместо batched CROSS JOIN |
| 4 | Upstash Redis 500K limit reached | (rate limiting off) | **manual — Askar billing dashboard** |
| 5 | `tenant_settings = 0` для всех 7 tenants | (per-tenant config) | auto-create в `register_tenant` + backfill 7 строк |
| 6 | CSV exports без UTF-8 BOM | `/admin/export/*` | helper `_csv_response` с BOM + charset=utf-8 |
| 7 | Duplicate Operation ID в OpenAPI | `/positions/{id}/recommended-courses` | удалён дубликат из `recommendations_router.py` |
| 8 | `/audit/logs` 500 для tenant-admin | `/audit/logs` | `AuditLogResponse.resource_id: UUID \| str \| None` |
| (extra) | `courses.delivery_type` schema drift | `/admin/dashboard` | alembic 0054 + прямой ALTER TABLE |

## Commits

1. `b6ed97d` fix(p0): 5 critical production bugs fixed in single batch
2. `c2797ab` fix(auth): demo-login also needs set_config RLS bypass
3. `66fa6a5` fix(db): alembic migration 0054 add courses.delivery_type

## Live verification (финальная, после deploy 66fa6a5)

```
[1] /auth/superadmin-login: 200 OK
[2] /admin/dashboard: 200 OK
[3] /auth/register: 200 OK
[4] /auth/demo-login student: 200 OK
[5] /auth/demo-login methodologist: 200 OK
[6] /admin/export/users: 200 OK, Content-Type=text/csv; charset=utf-8
[7] /audit/logs tenant-admin: 200 OK
[8] Duplicate Operation ID warnings: 0
[9] tenant_settings rows: 7
[10] methodologist users: > 0
```

Все 8 API-проверок зелёные. Deploy: `ffb7a0a`, `c2797ab`, `66fa6a5` на master.

## Manual action items для Askar

1. **🔴 Сменить пароль superadmin** — сейчас `admin@kamilya.kz / QA-Reset-2026-Xk9m`
   (сменил сам для QA, нужно вернуть свой).
2. **🔴 Upstash Redis upgrade** — нужен доступ к Upstash billing dashboard,
   поднять tier с free на pay-as-you-go (~$10/month за 10M requests).
   Без этого rate limiting и auth_sessions остаются в fallback.

## Что осталось на следующий ход

- Schema drift в других таблицах (User, Enrollment, Position, etc.)
- `/admin/stats` показывает только superadmin context (1 user) — нужна отдельная platform query
- `/admin/trial-usage` → 403 для superadmin (для какой роли?)
- Auth-bypass review на `/auth/generate-code` (security)
- Frontend smoke через Vercel preview URL (visual_page + Playwright)
# Презентации Kamilya LMS

## Живая демонстрация продукта

Актуальный runbook для видеозвонка с финансовой организацией:
`2026-08-11_video-call-product-demo-runbook-ru.md`.

Предыдущая редакция живой демонстрации сохранена как историческая:
`2026-07-28_live-product-demo-runbook-ru.md`.

Документ содержит отраслевой контекст ИБ и ПОД/ФТ, подготовку demo-данных,
основной сценарий на 30–35 минут, сокращённую версию на 12–15 минут, точный
текст сопровождения, ожидаемый результат, резервные действия при сбоях и
ограничения продуктовых claims.

## Автономная презентация для финансовых организаций

Готовый файл:
`../../deliverables/kamilya-finance-kz-ib-podft-executive-deck.html`.

Десять слайдов ведут от управленческой проблемы к живой демонстрации, результату
для руководителя, HR и ИБ/комплаенса, масштабированию, клиентскому контуру в
Казахстане и пилоту на одном документе. Файл не использует внешние библиотеки и
работает локально. Управление: стрелки, `Space`, `Home`, `End`, `O`, `N`, `S`,
`F`, `?` и свайпы. Встроены текст ведущего, официальные источники и режим печати.

Исходный отраслевой план и gate следующих редакций:
`2026-08-13_finance-presentation-backlog-ru.md`.

## Клиентская sales-story

Файл: `2026-07-27_kamilya-lms-sales-story.html`

Девять экранов показывают путь от документов компании до курса, назначения,
прохождения, журнала и сертификата. Управление: стрелки, `Space`, `Home`,
`End`, `O` и свайпы. Параметры `slide=N` и `autoplay=0` позволяют открыть
нужный экран и остановить автоматический показ.

## Интерактивный walkthrough методолога

Файл: `2026-07-14_methodologist-product-walkthrough.html`

Презентация работает локально без внешних библиотек и API. Для предсказуемой навигации по query-параметрам откройте её через локальный HTTP-сервер или как обычный HTML-файл в Chromium/Chrome/Edge.

### Режимы

- `mode=sales` — короткий клиентский сценарий из 8 экранов; используется по умолчанию;
- `mode=full` — подробный продуктовый walkthrough из 15 экранов.

Примеры:

```text
2026-07-14_methodologist-product-walkthrough.html?mode=sales
2026-07-14_methodologist-product-walkthrough.html?mode=full
2026-07-14_methodologist-product-walkthrough.html?mode=sales&slide=3
```

### Управление

- `←` / `→` — предыдущий и следующий экран;
- `Space` — запустить или остановить автопоказ;
- `O` — обзор всех экранов;
- `C` — скрыть элементы управления;
- `M` — переключить короткий и полный режимы;
- `Home` / `End` — первый и последний экран;
- свайп — навигация на сенсорном устройстве.

Текущий режим и номер экрана сохраняются в URL. Ссылку можно отправить сразу на нужный кадр.

### Просмотр на телефоне

- В портретной ориентации рабочий экран показывается отдельно от крупного текстового пояснения и практического результата.
- В горизонтальной ориентации презентация использует почти всю высоту для продуктового интерфейса, а компактное управление располагается под кадром.
- Обзор этапов открывается на весь экран. Масштабирование страницы браузером не блокируется.

### Режим записи ролика

Откройте файл с query-параметрами:

```text
2026-07-14_methodologist-product-walkthrough.html?mode=sales&record=1
```

Режим `record=1` включает автопоказ, скрывает управляющие элементы и курсор. Для ручного запуска без элементов управления используйте `?clean=1`, для запуска с конкретного экрана — `?slide=5`.

Рекомендуемый формат записи: Chromium, viewport `1920×1080`, без панели закладок и уведомлений. Короткая версия рассчитана примерно на 1 минуту, полная — примерно на 1,5 минуты без голосовой дорожки. Для ролика с озвучкой длительности экранов следует синхронизировать с финальным текстом диктора.

### Ограничение claims

Показанный журнал фиксирует прохождение корпоративных курсов. Сертификат является продуктовым сертификатом Kamilya LMS. Презентация не утверждает, что они заменяют обязательный журнал инструктажей по технике безопасности, электронную подпись или юридически значимый документ.

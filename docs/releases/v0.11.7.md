# Release Notes — 0.11.7

**Release date:** 2026-09-26

**Product version:** 0.11.7

**Git tag:** `v0.11.7`

## Fixed

- Assessment explanations now classify the exact source claim selected for the
  question. A deadline mentioned elsewhere in the same evidence paragraph no
  longer turns an unrelated action or rule into a false `срок` label.
- Expected generation acknowledgements such as source reuse and mixed-language
  confirmation no longer appear in the browser console as generation failures.
  Unexpected failures remain logged and keep the existing user-facing error.

## Verification

- Assessment axis, contract and semantic suites: 88 passed.
- AI generation frontend workflow and source-reuse suites: 30 passed.
- TypeScript typecheck, focused Python Ruff checks and `git diff --check`:
  passed.
- Exact-SHA CI, immutable image, frontend artifact, synchronized runtime
  readback and synthetic production acceptance remain release gates.

## Operations

- Database migration: none.
- Required backend scope: API and AI worker on the same immutable image.
- Required frontend scope: CT137 native Next.js release from the exact accepted
  SHA through the digest-bound release controller.
- No provider plan, billing, DNS or infrastructure-capacity change.

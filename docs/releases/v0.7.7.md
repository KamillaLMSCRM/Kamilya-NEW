# Release Notes — 0.7.7

**Release date:** 2026-09-19
**Product version:** 0.7.7
**Git tag:** `v0.7.7`
**Deployed commit:** pending exact-SHA release gate

## Summary

Предварительная оценка структуры курса теперь прямо обозначена как ориентир.
Она помогает методисту до запуска, но не выдаёт приблизительное число уроков за
жёсткий предел для последующего полного анализа документа.

## Added

- No new product capability.

## Changed

- Russian, Kazakh and English planning copy now uses “about / approximately”
  semantics for the estimated lesson count.

## Fixed

- Removed the misleading “up to N lessons” wording that could contradict the
  authoritative structure produced after complete source parsing.

## Security

- No authentication, authorization, tenant isolation or secret-handling policy
  change.

## Upgrade / deployment notes

- Database migrations: not required.
- API and worker behavior: unchanged.
- Environment variables added/changed: none.
- Provider routes, model configuration, DNS, infrastructure and billing:
  unchanged.
- Deployment scope: API, all three application workers and the native production
  frontend are synchronized to one immutable product-version identity. Backend
  behavior is unchanged; the backend rollout advances release metadata only.

## Verification

- Focused AI-generation page regression: `22 passed`.
- Complete frontend suite: `115` files / `619` tests passed.
- TypeScript, ESLint, production build and `git diff --check` passed.
- Production browser acceptance: pending exact-SHA CT137 deployment.

## Rollback

Rollback plan: atomically restore immutable native frontend release `v0.7.6`.
There is no data migration or schema caveat.

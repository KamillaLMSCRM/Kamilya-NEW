[tox]
envlist = py312
skipsdist = true

[testenv]
allowlist_externals = poetry,pytest
commands =
    poetry install --no-interaction --no-ansi
    poetry run pytest -xvs --cov=app --cov-report=term-missing
